export enum DeskboardOptionMenu {
  comics, // 0
  reports, // 1
  wallet // 2
}

export enum typeSearchCatalogue {
  publication,
  collection,
}

export enum typeManagueRoles {
  users,
  roles,
}

export enum DashboardGeneralOptionMenu {
  categories,
  tags
}

export enum TypeAmount {
  percent,
  normal,
  currency
}
